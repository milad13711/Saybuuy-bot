# استقرار بات روی VPS (اجرای ۲۴ساعته)

## اول، یه نکته‌ی مهم و شفاف

من به هیچ سرور بیرونی (VPS) با SSH وصل نمی‌شم — نه ابزاری برای اتصال SSH دارم و نه اطلاعات ورود (رمز عبور/کلید خصوصی) سرور رو ازت می‌گیرم؛ حتی اگه بدی، وارد کردن این‌جور اطلاعات محرمانه توسط من ممنوعه (هم برای امنیت خودت). پس نمی‌تونم مستقیم «برم روی سرورت و خودم انجامش بدم».

اما یه راه تقریباً هم‌ارز آماده کردم: یک اسکریپت واحد (`deploy.sh`) که **تمام کارها رو خودش انجام می‌ده** — فقط باید یک بار آپلودش کنی و یک دستور بزنی.

## چیکار باید بکنی (حدود ۲ دقیقه)

1. یک VPS با اوبونتو/دبیان تهیه کن (اگه نداری، هر ارائه‌دهنده‌ای مثل آرون/لیارا/Hetzner و... کافیه؛ کمترین پلن هم برای این بات کفایت می‌کنه).
2. فایل `deploy.sh` رو روی سرور آپلود کن. یکی از این روش‌ها:
   - از کامپیوتر خودت: `scp deploy.sh root@IP_SERVER:/root/`
   - یا از طریق پنل هاست (اگه File Manager وب داره)، محتوای فایل رو کپی/پیست کن.
3. وارد سرور شو (`ssh root@IP_SERVER`) و اجرا کن:
   ```bash
   sudo bash deploy.sh
   ```
4. وقتی توکن بات رو ازت پرسید (`1712993668:ZdLNv6zonpvBJH51IuH8AjRbEfviJKHjwig`)، وارد کن و Enter بزن.

همین. اسکریپت خودش:
- پایتون و ابزارهای لازم رو نصب می‌کنه
- پروژه رو در `/opt/saybuuy-bale-bot` می‌سازه
- یک `systemd service` می‌سازه که بات رو ۲۴ساعته اجرا نگه می‌داره — یعنی اگه سرور ری‌استارت بشه یا بات کرش کنه، خودش خودکار دوباره بالا میاد (بدون نیاز به نگه داشتن ترمینال باز، برخلاف حالت روی کامپیوتر شخصی)

## دستورات مدیریتی بعد از نصب

```bash
systemctl status saybuuy-bale-bot      # وضعیت فعلی
journalctl -u saybuuy-bale-bot -f      # دیدن لاگ زنده
systemctl restart saybuuy-bale-bot     # ری‌استارت
systemctl stop saybuuy-bale-bot        # توقف
```

برای تغییر متن‌ها/قیمت‌ها:
```bash
nano /opt/saybuuy-bale-bot/messages.py
systemctl restart saybuuy-bale-bot
```

## اگه می‌خوای من مستقیم‌تر کمک کنم

اگه یه پنل مدیریتی تحت وب داری (مثلاً پنل هاستینگ، یا یه ابزار مثل Webmin/cPanel) که از طریق مرورگر بازش می‌کنی، من می‌تونم با مرورگر (Claude in Chrome) توش کلیک کنم و فایل آپلود کنم — چون اون یه صفحه‌ی وب عادیه، نه یه اپ ترمینال. کافیه لینک و دسترسیش رو بگی.

اگه نه، همین یک اسکریپت + یک دستور، سریع‌ترین و امن‌ترین راهه.
